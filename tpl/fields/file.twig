{% extends field.layout %}

{% block control %}

    <input type="file"
           id="{{ prefix }}-{{ field.form }}-field-{{ field.id }}"
           name="{{ prefix }}[{{ field.form }}][{{ field.id }}]">

{% endblock %}
