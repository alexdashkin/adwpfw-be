{% extends 'field.twig' %}

{% block control %}

    <input type="password" class="form-control"
           id="{{ prefix }}-field-{{ field.id }}"
           name="{{ prefix }}[{{ field.id }}]"
           value="{{ field.value }}">

{% endblock %}
