{% extends 'layouts/' ~ field.layout ~ '.twig' %}

{% block control %}

    <textarea class="{{ field.class }}"
              id="{{ prefix }}-{{ field.form }}-field-{{ field.id }}"
              name="{{ prefix }}[{{ field.form }}][{{ field.id }}]">{{ field.value }}</textarea>

{% endblock %}
