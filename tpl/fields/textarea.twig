{% extends 'field.twig' %}

{% block control %}

    <textarea class="form-control" id="{{ prefix }}-field-{{ field.id }}" name="{{ prefix }}[{{ field.id }}]">

        {{ field.value }}

    </textarea>

{% endblock %}
