class {{ id }} extends WP_Widget
{
    public function __construct()
    {
        parent::__construct('{{ id }}', '{{ name }}');
    }

    public function widget($args, $instance)
    {
        do_action('{{ id }}', $args, $instance);
    }
}
